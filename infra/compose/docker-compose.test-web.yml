services:
  test:
    image: node:19.8
    build: ../../frontend
    volumes:
      - ../../frontend:/app
    working_dir: /app
    command: bash -c "npm i && node_modules/.bin/eslint . --ext .js --ext .jsx"
